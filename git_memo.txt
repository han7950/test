push
git status   		-- 씽크
git add .		-- 씽크후 변경 사항 트렉킹
git commit -m "xxx"   	-- ~바뀌었는지 설명 
git push origin master 	-- push 전에 pull이 있다를 기억.-- 여기서 pull 없이 push 해도 되는지 확인.




pull
local에 받아올 폴더 만들고, Git Bash Here

git init
git remote add origin https://github.com/bismuthfoundation/Bismuth  - 주소줄만...ㅎ

git pull origin master  -- 이렇게 안하면 https:// 에서 editing 한 것은 덮어 쓰기가 되어져 버린다. 
   그래서 push 전엔 pull이 있다가 중요하다.


 





https://www.youtube.com/watch?v=tC8Xj_Bf8Fw&t=15s
	이곳에 있는 강의자료를 정리한 것.


gitbash  설치
https://gitforwindows.org/


Repository 생성
https://github.com/

login 후 new repository


내 컴에 로컬 폴더 생성  우측버튼  --git bash here

git init 
git romete add origin https://github.com/han7950/test 
git pull origin master  -- push 전엔 pull 이 있다.
	-- 일단 끌어와서 싱크 하고 그걸 정리 해서 		push?? 해야 안 날려 먹는다.??
git status -- 현제 local and git과의 상태를(씽크) Check!

git add .  -- 변경 사항 트렉킹 

git commit -m "Hello Github"  -- 나 또는 남이 알 수 있게 내용을 달아준다. 

git push origin +master  -- 실제로 git 에 push!

pull 
    









*

https://www.youtube.com/watch?v=tC8Xj_Bf8Fw&t=15s


$ git remote add origin ?https://github.com/han7950/test.git

04. Push 전에 Pull이 있다.

git pull origin master   --- 지어 해놓은 Repository에서 끌어옵니다.

git status  --- 현재 git 상태를 check

git add .변경된 파일들을 모두 Tracking

git commit -m "message" -- 인식 할 수 있는 commit을 달아줍니다.

git push origin +master  --  실제로 Git에 POUSH를 요청












